import ChatContainer from "@/components/ChatContainer";

const Home = () => {
  return (
    <section className="w-full h-full p-10">
      <ChatContainer />
    </section>
  );
};

export default Home;
